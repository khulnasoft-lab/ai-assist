---
name: Claude 3 ReAct Chat agent
model:
  name: claude-3-5-sonnet@20240620
  params:
    model_class_provider: litellm
    custom_llm_provider: vertex_ai
    temperature: 0.0
    max_tokens: 2_048
    max_retries: 1
unit_primitives:
  - duo_chat
prompt_template:
  # WARN: Do NOT construct prompts here. ReAct agent prompts are dynamically composed as per request.
  # TODO: Remove the `prompt_template` section.
  system: |
    {% include 'chat/react/system.jinja' %}
params:
  # Overriding the `AIGW_GOOGLE_CLOUD_PLATFORM__PROJECT` config, since Sonnet 3.5
  # is only available in a handful of regions. See
  # https://cloud.google.com/vertex-ai/generative-ai/docs/partner-models/use-claude#regions
  vertex_location: us-east5
  timeout: 60
  stop:
    - "Observation:"
